<h1>Livres dans ma bibliothèque</H1>
<% @books.each do |book| %>
  <%= book.title %>
  <%= book.author %>
  <%= book.year %>
  <%= book.resume %>
  <%= book.price %>
  <%= link_to 'show', book_path(book) %>
  <%= link_to 'book delete',
  book_path(book), class: 'btn btn-gradient' ,
  method: :delete,
  data: { confirm: 'Are you sure?' } %>
  <br>
<% end %>
<%= link_to 'Back to Home', root_path %>
